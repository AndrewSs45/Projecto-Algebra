{
  "name": "chess-physics",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx http-server . -p 8080 -c-1",
    "build": "mkdir -p dist && cp *.html dist/ && cp -r js dist/ 2>/dev/null || true && cp -r public/* dist/ 2>/dev/null || true",
    "preview": "npx http-server dist -p 8080 -c-1"
  },
  "author": "Chess Physics Team",
  "license": "MIT"
}
